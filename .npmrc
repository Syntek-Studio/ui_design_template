# npm configuration for @syntek-studio/ui
#
# This file configures npm package manager behaviour for the component library.
# All settings are applied automatically during install and build processes.
#
# Configuration categories:
#   - Engine/Version Enforcement
#   - Package Management
#   - Registry Settings
#   - Peer Dependency Handling
#

# ============================================================================
# ENGINE & VERSION ENFORCEMENT
# ============================================================================

# Enforce specific Node.js and npm versions
# If set to true, npm will exit with an error if the Node version does not
# match the requirements defined in engines field of package.json
#
# Current requirements: Node 18+ (TypeScript, modern ES features)
# Set to true to prevent build failures due to version mismatches
#
# @see package.json engines field
engine-strict=true

# ============================================================================
# PACKAGE MANAGEMENT
# ============================================================================

# Controls whether to save exact versions (false) or version ranges (true)
# false = allows minor/patch updates (e.g., 1.2.x)
# true = locks exact version (e.g., 1.2.3 only)
#
# Set to false for flexibility; package-lock.json provides reproducible builds
save-exact=false

# Generate package-lock.json to ensure reproducible installs
# Essential for CI/CD pipelines and team collaboration
# All team members and CI systems will install identical dependencies
package-lock=true

# ============================================================================
# REGISTRY SETTINGS
# ============================================================================

# Use default npm registry for all public packages
# All production and development dependencies are fetched from npm registry
#
# @see https://registry.npmjs.org/
registry=https://registry.npmjs.org/

# ============================================================================
# PEER DEPENDENCY HANDLING
# ============================================================================

# Do not automatically install peer dependencies
# Set to false to enforce strict peer dependency compatibility
#
# This package declares optional peer dependencies:
#   - react (^19.2.3)
#   - react-dom (^19.2.3)
#   - react-native (>=0.83.1) [optional]
#   - nativewind (^4.2.1)
#   - react-native-reanimated (>=4.2.1) [optional]
#   - react-native-safe-area-context (>=5.6.2) [optional]
#   - react-native-web (>=0.21.2) [optional]
#
# Setting to false ensures consuming projects install peer dependencies
# according to their own requirements, preventing version conflicts.
#
# Use true only temporarily to bypass peer dep warnings during development.
legacy-peer-deps=false
