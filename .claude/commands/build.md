---
allowed-tools: Bash(npm run build:*), Bash(npm run type-check:*)
description: Build the library and type-check
---

Run the full build process:

1. Run `npm run build` to build the library
2. Run `npm run type-check` to verify TypeScript types

Report any errors found. If the build succeeds, show the output file sizes.
